---
name: flow-generate
description: "Generate Salesforce Flow metadata (Screen / Autolaunched / Record-Triggered before-save and after-save / Scheduled) using the MCP `execute_metadata_action` 3-step pipeline (fetchGroundedObjectMetadata → flowElementSelection → flowElementGeneration). Use this skill for any Flow generation. Trigger phrases: 'create a flow', 'build a flow that', 'when a record is created send', 'trigger daily at', 'send an email when', 'update the field when', 'automate this with a flow', 'generate flow XML', 'autolaunched flow for'. Pairs with `flow-patterns` (reference). Do NOT trigger for Apex automation — use `apex-generate`."
argument-hint: "[flow type and intent, e.g. 'before-save flow on Lead to populate territory' or 'screen flow for case escalation']"
---

# <span data-proof="authored" data-by="ai:claude">/flow-generate</span>

> **<span data-proof="authored" data-by="ai:claude">Principles enforced:</span>** <span data-proof="authored" data-by="ai:claude">1 (preserve the quality ceiling), 4 (spec is the artifact), 2 (verifiability). See</span> <span data-proof="authored" data-by="ai:claude">`PRINCIPLES.md`.</span>

## <span data-proof="authored" data-by="ai:claude">Copy-paste-to-agent</span>

```
Generate a Salesforce Flow by running the strict 3-step MCP pipeline. The pipeline IS the
contract: fetchGroundedObjectMetadata → flowElementSelection → flowElementGeneration.
Loop on flowElementGeneration with the same operationId until isComplete=true. Never
hand-write Flow XML. Never skip steps. After XML is returned, validate with
sf code-analyzer and dispatch flow-governor-monitor + flow-complexity-analyzer review
agents in parallel. Bulkify per Salesforce flow best practices: no DML/SOQL inside loops.
```

## <span data-proof="authored" data-by="ai:claude">When to use</span>

<span data-proof="authored" data-by="ai:claude">Use for any Flow type:</span>

* **<span data-proof="authored" data-by="ai:claude">Screen Flow</span>** <span data-proof="authored" data-by="ai:claude">— user-guided, multi-step data collection</span>

* **<span data-proof="authored" data-by="ai:claude">Autolaunched Flow</span>** <span data-proof="authored" data-by="ai:claude">— background processing, called from Apex / Process / button</span>

* **<span data-proof="authored" data-by="ai:claude">Record-Triggered Flow (before-save)</span>** <span data-proof="authored" data-by="ai:claude">— same-record field updates, validation</span>

* **<span data-proof="authored" data-by="ai:claude">Record-Triggered Flow (after-save)</span>** <span data-proof="authored" data-by="ai:claude">— related-record DML, async actions</span>

* **<span data-proof="authored" data-by="ai:claude">Scheduled Flow</span>** <span data-proof="authored" data-by="ai:claude">— recurring or time-based batch automation</span>

<span data-proof="authored" data-by="ai:claude">If the request is ambiguous between a Flow and an Apex trigger, route via</span> <span data-proof="authored" data-by="ai:claude">`/sf-brainstorm`</span> <span data-proof="authored" data-by="ai:claude">first — the declarative-vs-code matrix lives there.</span>

***

## <span data-proof="authored" data-by="ai:claude">The pipeline is non-negotiable</span>

<span data-proof="authored" data-by="ai:claude">Flow XML is generated by running the</span> **<span data-proof="authored" data-by="ai:claude">`execute_metadata_action`</span>** <span data-proof="authored" data-by="ai:claude">MCP tool with three sequential</span> <span data-proof="authored" data-by="ai:claude">`action`</span> <span data-proof="authored" data-by="ai:claude">values.</span> **<span data-proof="authored" data-by="ai:claude">Never hand-write Flow XML.</span>** <span data-proof="authored" data-by="ai:claude">Hand-written Flow XML fails deployment in subtle ways (missing region IDs, wrong reference syntax, broken element ordering).</span>

### <span data-proof="authored" data-by="ai:claude">Step 1 —</span> <span data-proof="authored" data-by="ai:claude">`fetchGroundedObjectMetadata`</span>

| <span data-proof="authored" data-by="ai:claude">Input</span>              | <span data-proof="authored" data-by="ai:claude">Type</span>              | <span data-proof="authored" data-by="ai:claude">Notes</span>                                                                                                                                                                              |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <span data-proof="authored" data-by="ai:claude">`userPrompt`</span>       | <span data-proof="authored" data-by="ai:claude">string (required)</span> | <span data-proof="authored" data-by="ai:claude">The user's natural-language request</span>                                                                                                                                                |
| <span data-proof="authored" data-by="ai:claude">`inflightMetadata`</span> | <span data-proof="authored" data-by="ai:claude">array (required)</span>  | <span data-proof="authored" data-by="ai:claude">Custom objects/fields from the local sfdx project. Use</span> <span data-proof="authored" data-by="ai:claude">`[]`</span> <span data-proof="authored" data-by="ai:claude">if none.</span> |

<span data-proof="authored" data-by="ai:claude">Output:</span> <span data-proof="authored" data-by="ai:claude">`groundingMetadata`</span> <span data-proof="authored" data-by="ai:claude">(string — already serialized; pass directly to Step 2).</span>

### <span data-proof="authored" data-by="ai:claude">Step 2 —</span> <span data-proof="authored" data-by="ai:claude">`flowElementSelection`</span>

| <span data-proof="authored" data-by="ai:claude">Input</span>               | <span data-proof="authored" data-by="ai:claude">Type</span>              | <span data-proof="authored" data-by="ai:claude">Notes</span>                                                                                                                                                           |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <span data-proof="authored" data-by="ai:claude">`userPrompt`</span>        | <span data-proof="authored" data-by="ai:claude">string (required)</span> | <span data-proof="authored" data-by="ai:claude">Same value as Step 1</span>                                                                                                                                            |
| <span data-proof="authored" data-by="ai:claude">`groundingMetadata`</span> | <span data-proof="authored" data-by="ai:claude">string (required)</span> | <span data-proof="authored" data-by="ai:claude">Pass-through from Step 1; do</span> **<span data-proof="authored" data-by="ai:claude">not</span>** <span data-proof="authored" data-by="ai:claude">re-serialize</span> |
| <span data-proof="authored" data-by="ai:claude">`operationId`</span>       | <span data-proof="authored" data-by="ai:claude">string (required)</span> | <span data-proof="authored" data-by="ai:claude">Empty string</span> <span data-proof="authored" data-by="ai:claude">`""`</span> <span data-proof="authored" data-by="ai:claude">for the first call</span>              |

<span data-proof="authored" data-by="ai:claude">Output:</span> <span data-proof="authored" data-by="ai:claude">`operationId`</span> <span data-proof="authored" data-by="ai:claude">(carry to Step 3) +</span> <span data-proof="authored" data-by="ai:claude">`userOutput`</span> <span data-proof="authored" data-by="ai:claude">(reasoning to surface to the user).</span>

### <span data-proof="authored" data-by="ai:claude">Step 3 —</span> <span data-proof="authored" data-by="ai:claude">`flowElementGeneration`</span> <span data-proof="authored" data-by="ai:claude">(loop until complete)</span>

| <span data-proof="authored" data-by="ai:claude">Input</span>           | <span data-proof="authored" data-by="ai:claude">Type</span>              | <span data-proof="authored" data-by="ai:claude">Notes</span>                                                                         |
| ---------------------------------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------ |
| <span data-proof="authored" data-by="ai:claude">`operationId`</span>   | <span data-proof="authored" data-by="ai:claude">string (required)</span> | <span data-proof="authored" data-by="ai:claude">From Step 2 output</span>                                                            |
| <span data-proof="authored" data-by="ai:claude">`requestSource`</span> | <span data-proof="authored" data-by="ai:claude">string (required)</span> | <span data-proof="authored" data-by="ai:claude">`"A4V"`</span> <span data-proof="authored" data-by="ai:claude">for XML output</span> |

<span data-proof="authored" data-by="ai:claude">Output:</span> <span data-proof="authored" data-by="ai:claude">`isComplete`</span> <span data-proof="authored" data-by="ai:claude">(boolean) +</span> <span data-proof="authored" data-by="ai:claude">`result`</span> <span data-proof="authored" data-by="ai:claude">(string — final XML when</span> <span data-proof="authored" data-by="ai:claude">`isComplete=true`).</span>

**<span data-proof="authored" data-by="ai:claude">Loop discipline:</span>**

* <span data-proof="authored" data-by="ai:claude">A flow can have any number of elements (10–50+). Each call generates one element.</span>

* <span data-proof="authored" data-by="ai:claude">Call</span> <span data-proof="authored" data-by="ai:claude">`flowElementGeneration`</span> <span data-proof="authored" data-by="ai:claude">repeatedly with the</span> **<span data-proof="authored" data-by="ai:claude">same</span>**<span data-proof="authored" data-by="ai:claude"></span> <span data-proof="authored" data-by="ai:claude">`operationId`.</span>

* <span data-proof="authored" data-by="ai:claude">Do</span> **<span data-proof="authored" data-by="ai:claude">not</span>** <span data-proof="authored" data-by="ai:claude">pause, summarize mid-loop, or ask the user to confirm continuation.</span>

* <span data-proof="authored" data-by="ai:claude">Stop only when</span> <span data-proof="authored" data-by="ai:claude">`isComplete=true`</span> **<span data-proof="authored" data-by="ai:claude">or</span>** <span data-proof="authored" data-by="ai:claude">the call returns errors. There is no max iteration count.</span>

* <span data-proof="authored" data-by="ai:claude">When</span> <span data-proof="authored" data-by="ai:claude">`isComplete=true`, extract the XML from</span> <span data-proof="authored" data-by="ai:claude">`result`.</span>

***

## <span data-proof="authored" data-by="ai:claude">Step 0: Pre-implementation research (parallel, Principle 7)</span>

* <span data-proof="authored" data-by="ai:claude">Task</span> <span data-proof="authored" data-by="ai:claude">`sf-learnings-researcher(flow_intent)`</span> <span data-proof="authored" data-by="ai:claude">— has anyone solved this flow shape?</span> **<span data-proof="authored" data-by="ai:claude">Must-read.</span>**

* <span data-proof="authored" data-by="ai:claude">Task</span> <span data-proof="authored" data-by="ai:claude">`sf-repo-research-analyst(flow_intent)`</span> <span data-proof="authored" data-by="ai:claude">— what flows / process builders / Apex automations already touch this object?</span>

* <span data-proof="authored" data-by="ai:claude">Read</span> <span data-proof="authored" data-by="ai:claude">`skills/flow-patterns/SKILL.md`</span> <span data-proof="authored" data-by="ai:claude">for entry conditions, fault paths, naming.</span>

* <span data-proof="authored" data-by="ai:claude">Read</span> <span data-proof="authored" data-by="ai:claude">`skills/governor-limits/SKILL.md`</span> <span data-proof="authored" data-by="ai:claude">for flow-specific limits.</span>

***

## <span data-proof="authored" data-by="ai:claude">Post-generation validation (mandatory, Principle 2)</span>

<span data-proof="authored" data-by="ai:claude">Once the pipeline returns XML, do</span> **<span data-proof="authored" data-by="ai:claude">not</span>** <span data-proof="authored" data-by="ai:claude">declare success without these checks:</span>

1. **<span data-proof="authored" data-by="ai:claude">Persist the metadata.</span>** <span data-proof="authored" data-by="ai:claude">Save under</span> <span data-proof="authored" data-by="ai:claude">`force-app/main/default/flows/<FlowApiName>.flow-meta.xml`.</span>
2. **<span data-proof="authored" data-by="ai:claude">Static analysis.</span>** <span data-proof="authored" data-by="ai:claude">Run</span> <span data-proof="authored" data-by="ai:claude">`sf code-analyzer run --target <path> --json`</span> <span data-proof="authored" data-by="ai:claude">and remediate sev0/sev1/sev2.</span>
3. **<span data-proof="authored" data-by="ai:claude">Parallel review agents</span>** <span data-proof="authored" data-by="ai:claude">(Principle 1):</span>

   * <span data-proof="authored" data-by="ai:claude">Task</span> <span data-proof="authored" data-by="ai:claude">`flow-governor-monitor(flow_xml)`</span> <span data-proof="authored" data-by="ai:claude">— verifies no DML/SOQL in loops, fault paths present</span>

   * <span data-proof="authored" data-by="ai:claude">Task</span> <span data-proof="authored" data-by="ai:claude">`flow-complexity-analyzer(flow_xml)`</span> <span data-proof="authored" data-by="ai:claude">— flags excessive branching, unbounded loops</span>

   * <span data-proof="authored" data-by="ai:claude">Task</span> <span data-proof="authored" data-by="ai:claude">`process-automation-strategist(flow_xml)`</span> <span data-proof="authored" data-by="ai:claude">— confirms Flow vs Apex was the right call</span>
4. **<span data-proof="authored" data-by="ai:claude">Bulk-safe entry.</span>** <span data-proof="authored" data-by="ai:claude">For record-triggered flows, confirm entry conditions filter early to avoid wasted invocations across 200-record bulk imports.</span>
5. **<span data-proof="authored" data-by="ai:claude">Fault handling.</span>** <span data-proof="authored" data-by="ai:claude">Every flow with DML or callouts must have a fault path that captures and surfaces errors (Principle 1 — failures cannot be silent).</span>

***

## <span data-proof="authored" data-by="ai:claude">Hard constraints (every rule fails the deliverable if violated)</span>

| <span data-proof="authored" data-by="ai:claude">Rule</span>                                       | <span data-proof="authored" data-by="ai:claude">Why</span>                                                                                                                                                                                     |
| ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <span data-proof="authored" data-by="ai:claude">Never hand-write Flow XML</span>                  | <span data-proof="authored" data-by="ai:claude">Pipeline correctness is in the tool, not the LLM</span>                                                                                                                                        |
| <span data-proof="authored" data-by="ai:claude">Bulkify — no DML/SOQL inside Loop elements</span> | <span data-proof="authored" data-by="ai:claude">Governor limits multiply per record</span>                                                                                                                                                     |
| <span data-proof="authored" data-by="ai:claude">Entry conditions on record-triggered flows</span> | <span data-proof="authored" data-by="ai:claude">Skip records that don't need the flow; scale safety</span>                                                                                                                                     |
| <span data-proof="authored" data-by="ai:claude">Fault path on every DML / callout</span>          | <span data-proof="authored" data-by="ai:claude">Errors must surface, never silently swallow</span>                                                                                                                                             |
| <span data-proof="authored" data-by="ai:claude">Element naming follows project convention</span>  | <span data-proof="authored" data-by="ai:claude">E.g.,</span> <span data-proof="authored" data-by="ai:claude">`Get_<Object>_Details`,</span> <span data-proof="authored" data-by="ai:claude">`Decision_Check_<Field>`</span>                    |
| <span data-proof="authored" data-by="ai:claude">API name follows project convention</span>        | <span data-proof="authored" data-by="ai:claude">`<Object>_<Trigger>_<Action>`</span> <span data-proof="authored" data-by="ai:claude">(e.g.,</span> <span data-proof="authored" data-by="ai:claude">`Lead_BeforeSave_PopulateTerritory`)</span> |

***

## <span data-proof="authored" data-by="ai:claude">Output report</span>

```
Generated:
- force-app/main/default/flows/<FlowApiName>.flow-meta.xml

Pipeline:
- Step 1 (fetchGroundedObjectMetadata):  OK
- Step 2 (flowElementSelection):         OK (operationId=<id>)
- Step 3 (flowElementGeneration):        N iterations, isComplete=true

Analyzer:    {sev0=0, sev1=0, sev2=0, sev3=N}    or "unavailable: <reason>"
Reviews:
- flow-governor-monitor:        {findings count}
- flow-complexity-analyzer:     {findings count}
- process-automation-strategist:{verdict}

Verification Strategy: {how this flow will be tested — sf agent test, manual run, or recordTrigger fire}
```

***

## <span data-proof="authored" data-by="ai:claude">Hand off</span>

<span data-proof="authored" data-by="ai:claude">If the flow integrates with a broader feature, return to</span> <span data-proof="authored" data-by="ai:claude">`/sf-work`</span> <span data-proof="authored" data-by="ai:claude">for the next implementation step. If the work surfaced a non-obvious pattern (e.g., a recursion-prevention trick, a wait-element gotcha), run</span> <span data-proof="authored" data-by="ai:claude">`/sf-compound`.</span>

***

## <span data-proof="authored" data-by="ai:claude">Inspiration</span>

<span data-proof="authored" data-by="ai:claude">Adapted from</span> [<span data-proof="authored" data-by="ai:claude">`forcedotcom/afv-library/skills/generating-flow`</span>](https://github.com/forcedotcom/afv-library/tree/main/skills/generating-flow) <span data-proof="authored" data-by="ai:claude">(Apache-2.0). The upstream skill ships with detailed strict-XML constraints, region-ID rules, and reference-syntax tables. For the complete strict-mode constraints (especially around scheduled flow, screen-flow region targeting, and reference syntax), consult the upstream skill. This plugin's adaptation tightens the post-generation validation around the principles framework and the parallel-dispatch flow-* review agents.</span>